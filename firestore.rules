rules_version = '2';

// All resident, OTP, session, and content data is reached only through the API
// (Admin SDK bypasses these rules). Direct client access is denied by default.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
